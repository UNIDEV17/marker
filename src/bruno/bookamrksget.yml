info:
  name: bookamrksget
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/bookmarks?limit=1&cursor="
  params:
    - name: limit
      value: "1"
      type: query
    - name: cursor
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
